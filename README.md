# Contrast
Contrast Sensitivity task

# Github utilisation
git status = (va me dire si c'est à jour)

pour pouvoir mettre en ligne il faut déjà être à jour par rapport à ce qui est en ligne
donc il faut d'abord faire un git pull. Mais du coup, ça va potentiellement changer certains fichiers

git add ./ (ça va mettre tout ce qui est dans le dossier. Sinon préciser le nom des fichiers)

git commit -m "nom du changement"

git push (met en ligne) --> risque de fail, alors faire d'abord un git pull